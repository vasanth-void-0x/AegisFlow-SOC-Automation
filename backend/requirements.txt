fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.7.0
SQLAlchemy==2.0.36
alembic==1.14.0
httpx==0.28.1
python-dotenv==1.0.1
groq==1.6.0
pytest==8.3.4
pytest-asyncio==0.24.0

# ChromaDB and sentence-transformers are optional local enhancements.
# The deployable default uses the tested in-memory store and deterministic
# hashing embeddings, which require no heavyweight model packages.

# NOTE: the MCP server (app/mcp_server/) has its own isolated venv - see
# backend/requirements-mcp.txt and docs/MCP_SETUP.md. The `mcp` SDK pulls in
# a newer starlette than FastAPI supports, so it is NOT installed here.
